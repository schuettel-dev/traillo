<%=
  turbo_stream.replace(
    ListItem.model_name.collection, # => "list_items", the same identifier as used for turbo-frame in `_list_items.html.erb`
    partial: "list_items/list_items",
    locals: {
      list_items: ListItem.ordered
    }
  )
%>

<%= turbo_stream.replace ListItem.new, partial: "list_items/link_to_new" %>
